<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harjoitustyfinnkino">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Harjoitustyöfinnkino">
        <activity
            android:name=".LogIn"
            android:label="LogIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:label="Finnkino">
        </activity>
        <activity android:name="RegisterUser"
            android:label="Register">
        </activity>
        <activity android:name=".ShowMovies"
            android:windowSoftInputMode="stateVisible|adjustNothing"
            android:label="Finnkino">
        </activity>
        <activity android:name=".FindMovie"
            android:windowSoftInputMode="stateVisible|adjustNothing"
            android:label="Finnkino">
        </activity>
        <activity android:name=".CheckFavourites"
            android:windowSoftInputMode="stateVisible|adjustNothing">
        </activity>

    </application>

</manifest>
